import React from 'react';
import 'media-chrome';
import { Box } from '@mui/material';

interface PodcastGeneratorProps {
  videoUrl: string;
}

const PodcastGenerator: React.FC<PodcastGeneratorProps> = ({ videoUrl }) => {
  return (
    <Box>
      
    </Box>
  );
};

export default PodcastGenerator;
